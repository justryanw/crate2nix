[package]
name = "rustnix"
description = "An example project about rust and nix"
publish = false
version = "0.1.0"

edition = "2021"

[dependencies]
bevy = { version = "0.13", features = [ "wayland" ] }
